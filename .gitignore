.tmp
.pnpm-store

# ローカルの API キー等 (dotenvy が cwd 起点で walk-up して読む)。
# 詳細は README「env-var fallback」。
.env
.env.local
.env.*.local

# Node
node_modules
dist
*.tsbuildinfo

# Cargo build output (workspace root target/)
target

# Tauri CLI-generated schemas (per app)
examples/*/src-tauri/gen

# Claude Code
.claude/settings.local.json
.claude/scheduled_tasks.lock